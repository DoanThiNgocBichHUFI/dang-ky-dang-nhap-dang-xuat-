@using bai_1.Models;

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> 
        @{
            var user = Session["user"] as User;
        }
        @if (user != null)
        {
            <h2>Welcome @user.Username </h2>
        @Html.ActionLink("Dang xuat", "DangXuat", "DN");
        }
    </div>
</body>
</html>
